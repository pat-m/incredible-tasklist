{% extends 'base.html.twig' %}

{% block body %}
    {{ include('task/_form.html.twig', {'button_label': 'Mettre à jour la tâche'}) }}
    {{ include('task/_delete_form.html.twig') }}
    <a class="back" href="{{ path('task_index') }}">retour à la liste des tâches</a>
{% endblock %}
