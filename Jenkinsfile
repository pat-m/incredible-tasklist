pipeline {
    agent { docker { image 'php' } }

    stages {
        stage('Clone sources') {
           steps {
                sh 'php --version'
           }
        }
        stage('Prepare') {
            steps {
                sh 'php --version'
           }
        }
        stage('Test'){
            steps {
                sh 'php --version'
            }
        }
    }
}